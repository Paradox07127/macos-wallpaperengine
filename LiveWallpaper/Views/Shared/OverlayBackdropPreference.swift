/// Shared by every overlay preview. The key is persisted user state — renaming it resets the toggle.
enum OverlayBackdropPreference {
    static let showsWallpaperDefaultsKey = "Monitor.PreviewShowsWallpaper"
}
